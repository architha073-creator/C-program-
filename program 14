#include<stdio.h>
int main()
{
    int i=1,n,sum=0;
    printf("Enter positive integer:");
    scanf("%d",&n);
    while(i<=n)
    {
        sum=sum+i*i;
        i++;
    }
    printf("%d",sum);
    return 0;
}